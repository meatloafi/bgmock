spring.application.name=bank-service

# Bank configuration for testing
BANK_CLEARING_NUMBER=000001-TEST

# Default test properties
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=bank-service-test-group
