apiVersion: apps/v1
kind: Deployment
metadata:
  name: bankgood-bank
  labels:
    app: bankgood-bank
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bankgood-bank
  template:
    metadata:
      labels:
        app: bankgood-bank
    spec:
      containers:
      - name: bankgood-bank
        image: bankgood-bank:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bankgood-bank-service
spec:
  type: NodePort
  selector:
    app: bankgood-bank
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    nodePort: 30080
