# k8s/postgres/postgres-bank-a.yaml
# PostgreSQL for Bank A

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config-bank-a
  namespace: default
data:
  POSTGRES_DB: bank_a_db
  POSTGRES_USER: bank_a_user

---
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret-bank-a
  namespace: default
type: Opaque
stringData:
  POSTGRES_PASSWORD: password

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-bank-a
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-bank-a
  namespace: default
spec:
  serviceName: postgres-service-bank-a
  replicas: 1
  selector:
    matchLabels:
      app: postgres-bank-a
  template:
    metadata:
      labels:
        app: postgres-bank-a
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
              name: postgres
          envFrom:
            - configMapRef:
                name: postgres-config-bank-a
            - secretRef:
                name: postgres-secret-bank-a
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "bank_a_user", "-d", "bank_a_db"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "bank_a_user", "-d", "bank_a_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc-bank-a

---
apiVersion: v1
kind: Service
metadata:
  name: postgres-service-bank-a
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: postgres-bank-a
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres